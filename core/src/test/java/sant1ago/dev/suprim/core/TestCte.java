package sant1ago.dev.suprim.core;

import sant1ago.dev.suprim.annotation.entity.Entity;

/**
 * Test entity for CTE (Common Table Expression) tests.
 * Used as a placeholder for dynamically named CTEs.
 */
@Entity(table = "cte")
public class TestCte {
    // Empty class - just needs @Entity annotation for Table.of() validation
}
